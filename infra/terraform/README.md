# Terraform README

## Usage
- terraform init
- terraform apply -var="project_id=<id>" -var="region=us-central1"

## Destroy
terraform destroy

Note: Replace <your-github-repo> in main.tf with your repo like "owner/repo".

